{
  validate: function ({data, originalData, state, component, formName, index, controlRef, args, isChild}){
    var errors = {};
    //TODO: Insert your code for valication this form
    //if(data.name == undefined || data.name == ''){
    //  errors.name = 'This field is requered!';
    //}
    
    
    return errors;
  }
}